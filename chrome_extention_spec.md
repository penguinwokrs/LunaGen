chromeのエクステンション化の計画考えてください。

## 概要
マッチングアプリ、lunaの利便性を向上させるために、chromeエクステンションを開発します。
このエクステンションは、Lunaのために画面を拡張して
ユーザーがブラウザ上で直接メッセージの送受信、生成ボタンのショートカット、生成するためのプロンプトの設定などを行えるようにします。
すでにpythonのスクリプトがあるので、そこを参考にしながら作ってください。

## 前提
技術スタックとして、必ずPlasmoとreactを採用してください。
テストするときは、メッセージは実際行わずメッセージを送る直前の工程まで必ずテストしてください。

## 機能要件
1. 専用の設定ページの作成
    - ユーザーがエクステンションの設定を行うためのUIを提供
    - プロンプトのカスタマイズ、AIの生成に必要な設定(APY KEYなど)を保存できるようにする
    - 設定内容はローカルストレージに保存
2. 生成AIの設定
    - モデル一覧を作成してユーザーが選べるように設定画面を作成する
    - gemini, openAIのAPI keyそれぞれ入っていれば、選択肢が活性化させるような画面にする
3. メッセージ生成ボタンの追加
    - Lunaのチャット画面に「メッセージ生成」ボタンを追加
    - ボタンをクリックすると、設定されたプロンプトに基づいてAIがメッセージを生成し、チャット入力欄に自動で挿入



## 参考
- 作成時にテストしたりする時の相手のプロフィールは以下を使用
  - https://luna-matching.com/user/show/138217
  